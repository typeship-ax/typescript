// typeship — generated smoke tests for the specRevisions resource.
// Generated by typeship — https://typeship.dev — do not edit by hand.
import test from "node:test";
import assert from "node:assert/strict";
import { TypeshipClient } from "../dist/index.js";
import { startMock } from "./helper.mjs";

test("specRevisions.list GET /projects/{project_id}/spec_revisions", async () => {
  const mock = await startMock({ status: 200, contentType: "application/json", body: "{\"data\":[{\"id\":\"spec_6m1q8v4k2p9d7h3c\",\"object\":\"spec_revision\",\"project_id\":\"prj_4f8k2m7x9q1v6b3n\",\"sha256\":\"9f86d081884c7d659a2feaa0c55ad015a3bf4f1b2b0b822cd15d6c15b0f00a08\",\"size_bytes\":1,\"source\":{\"kind\":\"url\",\"url\":\"https://example.com\"},\"created_at\":\"2024-01-01T00:00:00Z\"}]}" });
  try {
    const client = new TypeshipClient({ baseUrl: mock.url, bearerToken: "test-token" });
    const result = await client.specRevisions.list("test-project_id", undefined);
    assert.equal(result.ok, true, JSON.stringify(result));
    const request = mock.requests[0];
    assert.equal(request.method, "GET");
    assert.equal(request.path.split("?")[0], "/projects/test-project_id/spec_revisions");
    assert.equal(request.headers["authorization"], "Bearer test-token");
  } finally {
    mock.close();
  }
});

test("specRevisions.retrieve GET /spec_revisions/{spec_revision_id}", async () => {
  const mock = await startMock({ status: 200, contentType: "application/json", body: "{\"id\":\"spec_6m1q8v4k2p9d7h3c\",\"object\":\"spec_revision\",\"project_id\":\"prj_4f8k2m7x9q1v6b3n\",\"sha256\":\"9f86d081884c7d659a2feaa0c55ad015a3bf4f1b2b0b822cd15d6c15b0f00a08\",\"size_bytes\":1,\"source\":{\"kind\":\"url\",\"url\":\"https://example.com\"},\"created_at\":\"2024-01-01T00:00:00Z\"}" });
  try {
    const client = new TypeshipClient({ baseUrl: mock.url, bearerToken: "test-token" });
    const result = await client.specRevisions.retrieve("test-spec_revision_id");
    assert.equal(result.ok, true, JSON.stringify(result));
    const request = mock.requests[0];
    assert.equal(request.method, "GET");
    assert.equal(request.path.split("?")[0], "/spec_revisions/test-spec_revision_id");
    assert.equal(request.headers["authorization"], "Bearer test-token");
  } finally {
    mock.close();
  }
});

test("specRevisions.retrieveContent GET /spec_revisions/{spec_revision_id}/content", async () => {
  const mock = await startMock({ status: 200, contentType: "application/json", body: "\"example\"" });
  try {
    const client = new TypeshipClient({ baseUrl: mock.url, bearerToken: "test-token" });
    const result = await client.specRevisions.retrieveContent("test-spec_revision_id");
    assert.equal(result.ok, true, JSON.stringify(result));
    const request = mock.requests[0];
    assert.equal(request.method, "GET");
    assert.equal(request.path.split("?")[0], "/spec_revisions/test-spec_revision_id/content");
    assert.equal(request.headers["authorization"], "Bearer test-token");
  } finally {
    mock.close();
  }
});
