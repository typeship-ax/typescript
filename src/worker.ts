// typeship — host this MCP server on Cloudflare.
// Generated by typeship — https://typeship.dev
// Deploy: npx wrangler deploy   (wrangler.toml is preconfigured)
// Callers' Authorization headers pass through to the API; hold no
// secrets here unless the server itself should authenticate
// (wrangler secret put TYPESHIP_TOKEN).
import { handleHttp } from "./mcp.js";

export default {
  fetch(request: Request): Promise<Response> {
    return handleHttp(request);
  },
};
