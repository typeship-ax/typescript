// typeship — files resource.
// Generated by typeship — https://typeship.dev

import { HttpCore, type RequestOptions } from "../core/http.js";
import {
  ResponseParseError,
  TransportError,
  UnexpectedApiError,
  ValidationError,
  BadRequestError,
  ForbiddenError,
  InternalServerError,
  NotFoundError,
  RateLimitedError,
  UnauthorizedError,
} from "../errors.js";
import type { FileId, FileResponse, FileResponseRead } from "../types.js";

export class FilesResource {
  constructor(private readonly _core: HttpCore) {}
  /**
   * Get a File
   *
   * Returns one bounded chunk of an immutable file: at most 24 KiB, as UTF-8 text or, for binary
   * bytes, base64. When next_cursor is not null, repeat the request with cursor and concatenate the
   * chunks in order. A file ID always returns the same bytes.
   * `GET /files/{file_id}`
   */
  async get(fileId: FileId, params?: FilesGetParams, options?: RequestOptions): Promise<FileResponseRead> {
    return this._core.requestData<FileResponseRead, FilesGetError>({
      method: "GET",
      path: `/files/${encodeURIComponent(String(fileId))}`,
      security: [{"apiKey":[]}],
      query: {
        cursor: params?.cursor,
      },
      errors: {
        "400": BadRequestError,
        "401": UnauthorizedError,
        "403": ForbiddenError,
        "404": NotFoundError,
        "429": RateLimitedError,
        "500": InternalServerError,
      },
      idempotent: true,
      schemaKey: "files.get",
      options,
    });
  }
}

export interface FilesGetParams {
  /** next_cursor from the preceding chunk of this file. */
  cursor?: string;
}

/** Typed errors `get` can throw. */
export type FilesGetError =
  | BadRequestError
  | UnauthorizedError
  | ForbiddenError
  | NotFoundError
  | RateLimitedError
  | InternalServerError
  | UnexpectedApiError
  | ResponseParseError
  | TransportError
  | ValidationError;
