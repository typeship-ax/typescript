// typeship — definitions resource.
// Generated by typeship — https://typeship.dev — do not edit by hand.

import { HttpCore, type ApiResult, type RequestOptions } from "../core/http.js";
import {
  TransportError,
  UnexpectedApiError,
  ValidationError,
  BadRequestError,
  ForbiddenError,
  NotFoundError,
  RateLimitedError,
  UnauthorizedError,
  UnprocessableEntityError,
} from "../errors.js";
import type {
  Definition,
  DefinitionId,
  DefinitionRead,
  DefinitionUpdateRequest,
} from "../types.js";

export class DefinitionsResource {
  constructor(private readonly _core: HttpCore) {}
  /**
   * Retrieve a Definition
   * `GET /definitions/{definition_id}`
   */
  async retrieve(
    definitionId: DefinitionId,
    options?: RequestOptions,
  ): Promise<ApiResult<DefinitionRead, DefinitionsRetrieveError>> {
    return this._core.request<DefinitionRead, DefinitionsRetrieveError>({
      method: "GET",
      path: `/definitions/${encodeURIComponent(String(definitionId))}`,
      errors: {
        "401": UnauthorizedError,
        "403": ForbiddenError,
        "404": NotFoundError,
        "429": RateLimitedError,
      },
      idempotent: true,
      schemaKey: "definitions.retrieve",
      options,
    });
  }

  /**
   * Update and resolve a Definition
   *
   * Resolves the complete document graph and records a new immutable revision before saving.
   * `PATCH /definitions/{definition_id}`
   */
  async update(
    definitionId: DefinitionId,
    body: DefinitionUpdateRequest,
    options?: RequestOptions,
  ): Promise<ApiResult<DefinitionRead, DefinitionsUpdateError>> {
    return this._core.request<DefinitionRead, DefinitionsUpdateError>({
      method: "PATCH",
      path: `/definitions/${encodeURIComponent(String(definitionId))}`,
      body,
      errors: {
        "400": BadRequestError,
        "401": UnauthorizedError,
        "403": ForbiddenError,
        "404": NotFoundError,
        "422": UnprocessableEntityError,
        "429": RateLimitedError,
      },
      schemaKey: "definitions.update",
      options,
    });
  }
}

/** Every error `retrieve` can produce, as a discriminated union. */
export type DefinitionsRetrieveError =
  | UnauthorizedError
  | ForbiddenError
  | NotFoundError
  | RateLimitedError
  | UnexpectedApiError
  | TransportError
  | ValidationError;

/** Every error `update` can produce, as a discriminated union. */
export type DefinitionsUpdateError =
  | BadRequestError
  | UnauthorizedError
  | ForbiddenError
  | NotFoundError
  | UnprocessableEntityError
  | RateLimitedError
  | UnexpectedApiError
  | TransportError
  | ValidationError;
