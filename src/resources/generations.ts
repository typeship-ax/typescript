// typeship — generations resource.
// Generated by typeship — https://typeship.dev — do not edit by hand.

import { HttpCore, type ApiResult, type RequestOptions } from "../core/http.js";
import { TransportError, UnexpectedApiError, ValidationError, ForbiddenError, NotFoundError, RateLimitedError, UnauthorizedError } from "../errors.js";
import type { Generation, GenerationId } from "../types.js";

export class GenerationsResource {
  constructor(private readonly _core: HttpCore) {}
  /**
   * Retrieve a generation
   *
   * Includes the generated files when the generation succeeded.
   * `GET /generations/{generation_id}`
   */
  async retrieve(generationId: GenerationId, options?: RequestOptions): Promise<ApiResult<Generation, GenerationsRetrieveError>> {
    return this._core.request<Generation, GenerationsRetrieveError>({
      method: "GET",
      path: `/generations/${encodeURIComponent(String(generationId))}`,
      errors: { "401": UnauthorizedError, "403": ForbiddenError, "404": NotFoundError, "429": RateLimitedError },
      idempotent: true,
      schemaKey: "generations.retrieve",
      options,
    });
  }

  /**
   * Fetch one file from a generation
   *
   * Raw file content, for generations whose output was too large to inline (files_omitted true). The generation's files_index lists valid paths.
   * `GET /generations/{generation_id}/file`
   */
  async retrieveFile(generationId: GenerationId, params: GenerationsRetrieveFileParams, options?: RequestOptions): Promise<ApiResult<string, GenerationsRetrieveFileError>> {
    return this._core.request<string, GenerationsRetrieveFileError>({
      method: "GET",
      path: `/generations/${encodeURIComponent(String(generationId))}/file`,
      query: { path: params.path },
      errors: { "401": UnauthorizedError, "403": ForbiddenError, "404": NotFoundError, "429": RateLimitedError },
      idempotent: true,
      schemaKey: "generations.retrieveFile",
      options,
    });
  }
}

/** Every error `retrieve` can produce, as a discriminated union. */
export type GenerationsRetrieveError = UnauthorizedError | ForbiddenError | NotFoundError | RateLimitedError | UnexpectedApiError | TransportError | ValidationError;

export interface GenerationsRetrieveFileParams {
  /** Repo-relative path inside the generated package. */
  path: string;
}

/** Every error `retrieveFile` can produce, as a discriminated union. */
export type GenerationsRetrieveFileError = UnauthorizedError | ForbiddenError | NotFoundError | RateLimitedError | UnexpectedApiError | TransportError | ValidationError;

