// typeship — organization resource.
// Generated by typeship — https://typeship.dev

import { HttpCore, type RequestOptions } from "../core/http.js";
import {
  ResponseParseError,
  TransportError,
  UnexpectedApiError,
  ValidationError,
  ForbiddenError,
  InternalServerError,
  RateLimitedError,
  UnauthorizedError,
} from "../errors.js";
import type { Organization, OrganizationRead } from "../types.js";

export class OrganizationResource {
  constructor(private readonly _core: HttpCore) {}
  /**
   * The organization behind the presented credentials
   *
   * Returns the organization associated with your credential. The Typeship CLI uses this endpoint
   * for `whoami`.
   * `GET /organization`
   */
  async get(options?: RequestOptions): Promise<OrganizationRead> {
    return this._core.requestData<OrganizationRead, OrganizationGetError>({
      method: "GET",
      path: "/organization",
      security: [{"apiKey":[]}],
      errors: {
        "401": UnauthorizedError,
        "403": ForbiddenError,
        "429": RateLimitedError,
        "500": InternalServerError,
      },
      idempotent: true,
      schemaKey: "organization.get",
      options,
    });
  }
}

/** Typed errors `get` can throw. */
export type OrganizationGetError =
  | UnauthorizedError
  | ForbiddenError
  | RateLimitedError
  | InternalServerError
  | UnexpectedApiError
  | ResponseParseError
  | TransportError
  | ValidationError;
