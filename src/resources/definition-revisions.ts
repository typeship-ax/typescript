// typeship — definitionRevisions resource.
// Generated by typeship — https://typeship.dev — do not edit by hand.

import { HttpCore, type ApiResult, type RequestOptions } from "../core/http.js";
import { paginate, PagePromise } from "../core/pagination.js";
import {
  TransportError,
  UnexpectedApiError,
  ValidationError,
  BadRequestError,
  ForbiddenError,
  NotFoundError,
  RateLimitedError,
  UnauthorizedError,
} from "../errors.js";
import type {
  DefinitionId,
  DefinitionRevision,
  DefinitionRevisionId,
  DefinitionRevisionList,
} from "../types.js";

export class DefinitionRevisionsResource {
  constructor(private readonly _core: HttpCore) {}
  /**
   * List Definition Revisions
   *
   * Immutable snapshots of the complete resolved document graph this Definition observed, newest
   * first. Content is available from the revision and document endpoints and is never embedded in a
   * list response.
   *
   * Auto-paginates: `for await (const item of …)` walks every page.
   * `GET /definitions/{definition_id}/revisions`
   */
  list(
    definitionId: DefinitionId,
    params?: DefinitionRevisionsListParams,
    options?: RequestOptions,
  ): PagePromise<DefinitionRevision, DefinitionRevisionsListError> {
    return paginate<DefinitionRevision, DefinitionRevisionsListError>(this._core, {
      method: "GET",
      path: `/definitions/${encodeURIComponent(String(definitionId))}/revisions`,
      query: {
        limit: params?.limit,
        cursor: params?.cursor,
      },
      errors: {
        "400": BadRequestError,
        "401": UnauthorizedError,
        "403": ForbiddenError,
        "404": NotFoundError,
        "429": RateLimitedError,
      },
      idempotent: true,
      schemaKey: "definitionRevisions.list",
      options,
    }, {
      style: "cursor",
      itemsField: "data",
      cursorParam: "cursor",
      nextCursorField: "next_cursor",
      hasMoreField: "has_more",
      limitParam: "limit",
    });
  }

  /**
   * Retrieve a Definition Revision
   *
   * Metadata for one immutable resolved document graph. Fetch its canonical content or individual
   * source documents from the content endpoints.
   * `GET /definition_revisions/{definition_revision_id}`
   */
  async retrieve(
    definitionRevisionId: DefinitionRevisionId,
    options?: RequestOptions,
  ): Promise<ApiResult<DefinitionRevision, DefinitionRevisionsRetrieveError>> {
    return this._core.request<DefinitionRevision, DefinitionRevisionsRetrieveError>({
      method: "GET",
      path: `/definition_revisions/${encodeURIComponent(String(definitionRevisionId))}`,
      errors: {
        "401": UnauthorizedError,
        "403": ForbiddenError,
        "404": NotFoundError,
        "429": RateLimitedError,
      },
      idempotent: true,
      schemaKey: "definitionRevisions.retrieve",
      options,
    });
  }
}

export interface DefinitionRevisionsListParams {
  /** Maximum number of resources to return. */
  limit?: number;
  /** Opaque cursor from the preceding page's next_cursor. */
  cursor?: string;
}

/** Every error `list` can produce, as a discriminated union. */
export type DefinitionRevisionsListError =
  | BadRequestError
  | UnauthorizedError
  | ForbiddenError
  | NotFoundError
  | RateLimitedError
  | UnexpectedApiError
  | TransportError
  | ValidationError;

/** Every error `retrieve` can produce, as a discriminated union. */
export type DefinitionRevisionsRetrieveError =
  | UnauthorizedError
  | ForbiddenError
  | NotFoundError
  | RateLimitedError
  | UnexpectedApiError
  | TransportError
  | ValidationError;
