// typeship — account resource.
// Generated by typeship — https://typeship.dev — do not edit by hand.

import { HttpCore, type ApiResult, type RequestOptions } from "../core/http.js";
import { TransportError, UnexpectedApiError, ValidationError, UnauthorizedError } from "../errors.js";
import type { User } from "../types.js";

export class AccountResource {
  constructor(private readonly _core: HttpCore) {}
  /**
   * The account behind the presented credentials
   * 
   * Returns the user that owns the presented API key. This is also the
   * identity endpoint the generated typeship CLI's `whoami` calls.
   * `GET /me`
   */
  async whoami(options?: RequestOptions): Promise<ApiResult<User, AccountWhoamiError>> {
    return this._core.request<User, AccountWhoamiError>({
      method: "GET",
      path: "/me",
      errors: { "401": UnauthorizedError },
      idempotent: true,
      schemaKey: "account.whoami",
      options,
    });
  }
}

/** Every error `whoami` can produce, as a discriminated union. */
export type AccountWhoamiError = UnauthorizedError | UnexpectedApiError | TransportError | ValidationError;

