// typeship — usage resource.
// Generated by typeship — https://typeship.dev — do not edit by hand.

import { HttpCore, type ApiResult, type RequestOptions } from "../core/http.js";
import { TransportError, UnexpectedApiError, ValidationError, UnauthorizedError } from "../errors.js";
import type { Usage } from "../types.js";

export class UsageResource {
  constructor(private readonly _core: HttpCore) {}
  /**
   * Retrieve usage for this account
   * 
   * What this account has consumed and what remains. A hosted generation can cost more than one unit: a project generating TypeScript, Python, and Go consumes three, so callers that would otherwise discover the limit by receiving a 402 can check first.
   * `GET /usage`
   */
  async retrieve(options?: RequestOptions): Promise<ApiResult<Usage, UsageRetrieveError>> {
    return this._core.request<Usage, UsageRetrieveError>({
      method: "GET",
      path: "/usage",
      errors: { "401": UnauthorizedError },
      idempotent: true,
      schemaKey: "usage.retrieve",
      options,
    });
  }
}

/** Every error `retrieve` can produce, as a discriminated union. */
export type UsageRetrieveError = UnauthorizedError | UnexpectedApiError | TransportError | ValidationError;

