# typeship — Cloudflare hosting config for the MCP server.
# Generated by typeship — https://typeship.dev
# Deploy with: npx wrangler deploy
name = "typeship-mcp"
main = "src/worker.ts"
compatibility_date = "2025-06-01"
compatibility_flags = ["nodejs_compat"]

# Server-held credentials are optional (callers' Authorization headers
# pass through). To set one: npx wrangler secret put TYPESHIP_TOKEN
